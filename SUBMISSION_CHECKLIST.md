# Activity 4 — 1-Hour Completion Checklist

1. Replace the roll number in README.md.
2. Run all 8 `.cpp` files locally; each contains 2 test cases.
3. Submit the 8 solutions on LeetCode.
4. For every **Accepted** result, save a screenshot in the same folder:
   `01-result.png` through `08-result.png`.
5. Update PROGRESS.md: change each status to `✅ Solved` and add time taken.
6. Run:
   git add .
   git commit -m "Add Activity 4: 8 LeetCode solutions with tests and docs"
   git push origin main
7. Verify the GitHub repository contains README, PROGRESS, 8 code files, 8 docs, and 8 Accepted screenshots.

Do not fake Accepted screenshots; use screenshots from your actual LeetCode submissions.
